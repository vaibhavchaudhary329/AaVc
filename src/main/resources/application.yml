spring:
  application:
    name: login
  datasource:
    url: jdbc:postgresql://dpg-d1lvo77fte5s73e0hflg-a.oregon-postgres.render.com:5432/postgres_login
    username: postgres_login_user
    password: 5Y1STzHkVz2u2jhSh8olf71qElRlF9r3
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope: openid, email, profile
            redirect-uri: "https://aavc.onrender.com/login/oauth2/code/google"
            provider:
              google: # This references the Google provider details (endpoints etc.)
                issuer-uri: https://accounts.google.com

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

logging:
  level:
    //root: DEBUG
    //org.springframework: DEBUG
    //org.hibernate.SQL: DEBUG
    org.springframework.security: DEBUG